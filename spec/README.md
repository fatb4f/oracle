# Specifications

OTEL schema specifications for `oracle` live here.

- `spec/schema.md`: human-readable schema contract description
- `spec/schema_contract.json`: machine-readable contract used by tests/runtime checks
