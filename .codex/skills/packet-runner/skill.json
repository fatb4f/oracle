{
  "skill_id": "packet-runner",
  "version": "0.1.2",
  "title": "Packet Runner (worktree + evidence)",
  "description": "Runs a packet contract in an isolated git worktree and emits evidence artifacts.",
  "entrypoint": "scripts/run_packet.py",
  "inputs": {
    "contract_path": {
      "type": "string",
      "required": true,
      "description": "Path to the packet contract JSON (e.g., .codex/packet/examples/packet-001-worktree-collab.json)"
    }
  }
}
